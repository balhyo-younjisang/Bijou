mixin photo(photo)
    div
        img(src="/"+photo.mainphotoUrl, width=100, height=100)
       
        h4 
            a(href=`/photos/${photo.id}`)=photo.title
        p=photo.description
        ul
            each hashtag in photo.hashtags
                li=hashtag
        p=`$${photo.price}`
        hr